<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=categ%></title>
  </head>
  <body>
    <%- include('./partials/nav') %>
    <h1><%=categ%></h1>
    <ul>
      <% filteredCa.forEach((item,index)=>{ %>
      <li><%= item.name %></li>
      <li><%= item.price %></li>
      <li><%= item.rating %></li>
      <li><%= item.details %></li>
      <% }) %>
    </ul>
  </body>
</html>
